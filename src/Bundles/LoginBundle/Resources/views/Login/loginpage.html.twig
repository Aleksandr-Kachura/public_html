{% extends 'AppFrontEndBundle::layout.html.twig' %}

 {% block title %} <title>Photo Gallery</title> {% endblock %}




{%block content %}
    <div class="wrapper"> <!-- wrapper -->
		<div class="mainTop">
			<h1 class="logo"></h1>
			<div class="registration">
				<span class="topReg"></span>
                                {% if error %}
                                    {%  if error.message=='Bad credentials.' %}
                                        Ошибка доступа
                                    {% else %}
                                    {{ error.message }}
                                    {% endif %}
                                {% endif %}

				<form method="POST" action="{{ path('bundles_login_security_check') }}">
					<label for="in1">Username</label>
					<input type="text" name="_username" id="in1">
					<label for="in2">Login</label>
					<input type="password" name="_password" id="in2">
					<button autofocus>Вход</button>
				</form>			
				<a href="{{path('bundles_login_reg')}}" class="cada">Регистрация</a>
			</div> 
		</div> <!-- END mainTop -->
	<div class="page-buffer"></div>	
	</div> <!-- END wrapper -->

{% endblock %}